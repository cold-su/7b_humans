-- 7 Billion Humans (2235) --
-- 26: Budget Brigade 2 --

a:
if s == printer:
	takefrom s
endif
if n == worker and
 myitem == datacube:
	giveto n
endif
if w == worker and
 e == worker and
 s == worker:
	if myitem >= 50:
		giveto e
	endif
	if myitem < 50:
		giveto w
	endif
else:
	if w == worker and
	 myitem < 50:
		giveto w
	endif
	if e == worker and
	 myitem >= 50:
		giveto e
	endif
endif
if s == shredder and
 myitem == datacube:
	giveto s
endif
jump a


