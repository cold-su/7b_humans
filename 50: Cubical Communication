-- 7 Billion Humans (2235) --
-- 50: Cubical Communication --

mem1 = set 1
a:
if sw == mem1:
	b:
	takefrom s
	giveto se
	mem3 = calc mem3 + 1
	step w
	tell everyone ok
	if mem3 == 5:
		end
	endif
	mem2 = set 0
	c:
	if mem2 == 3:
		jump b
	else:
		listenfor ok
	endif
	mem2 = calc mem2 + 1
	jump c
endif
listenfor ok
mem1 = calc mem1 + 1
jump a


